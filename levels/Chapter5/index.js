import ChapterFiveGame from "./chapter5";
import { Text, View } from "react-native";

export default function ChapterFive() {
    return (
        <View style={{flex:1, justifyContent: "center", alignItems:"center"}}>
 
            <ChapterFiveGame/>
        </View>

    )
}